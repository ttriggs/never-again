<%= form_for(@restaurant) do |f| %>
  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% @restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %><br>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_area :address %><br>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_area :city %><br>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %><br>
  </div>

  <div class="field">
    <%= f.label :cuisine_id %>
    <%= f.select :cuisine_id, cuisines_to_array %><br>
  </div>


  <div class="actions">
    <%= f.submit "Create Restaurant", class: "small radius button" %>
  </div>

<% end %>
